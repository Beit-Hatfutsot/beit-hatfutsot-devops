apiVersion: apps/v1beta1
kind: Deployment
metadata: {name: back}
spec:
  replicas: 1
  template:
    metadata:
      labels: {app: back}
    spec:
      containers:
      - image: gcr.io/bh-org-01/mojp-dbs-back:f4c5a0c0b7c0d77faa8c07466f3fd57f8b7f0786
        name: back
        ports:
        - {containerPort: 80}
        - {containerPort: 8081}
        resources:
          requests: {cpu: 20m}
        volumeMounts:
        - {mountPath: /etc/bhs, name: etc-bhs, readOnly: true}
      nodeSelector: {cloud.google.com/gke-nodepool: pool-3}
      volumes:
      - name: etc-bhs
        secret: {secretName: etc-bhs}
---
apiVersion: v1
kind: Service
metadata: {name: back}
spec:
  ports:
  - {name: '80', port: 80}
  - {name: '8081', port: 8081}
  selector: {app: back}
