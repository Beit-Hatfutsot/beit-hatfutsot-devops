apiVersion: apps/v1beta1
kind: Deployment
metadata: {name: back}
spec:
  replicas: 1
  template:
    metadata:
      labels: {app: back}
    spec:
      containers:
      - image: gcr.io/bh-org-01/mojp-dbs-back:c13962b08fd950cd4ae1ed44622e765d0d0e844b
        name: back
        ports:
        - {containerPort: 80}
        - {containerPort: 8081}
        resources:
          requests: {cpu: 20m}
        volumeMounts:
        - {mountPath: /etc/bhs, name: etc-bhs, readOnly: true}
      volumes:
      - name: etc-bhs
        secret: {secretName: etc-bhs}
---
apiVersion: v1
kind: Service
metadata: {name: back}
spec:
  ports:
  - {name: '80', port: 80}
  - {name: '8081', port: 8081}
  selector: {app: back}
